---
title: "Supermarket Data - EDA"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(tidyverse)
path <- "/home/kishen/Documents/Data Science/Projects/Supermarket Sales Segmentation/Data/"
df <- read_csv(paste(path, "supermarket.csv", sep=""))
df <- tibble(df)
```


Preview data
```{r}
knitr::kable(head(df, 10), digits = 2, caption = "Supermarket data preview")
```


**Data Overview**
```{r cars}
summary(df)
```

```{r}
sapply(df, class)
```

**Missing Values**
```{r}
sum(is.na(df))
```
## Exploratory Data Analysis
```{r}
ggplot(data= df, aes(x= "Total"))+
  geom_histogram(binwidth = 0.5)
```

